# Default example

This deploys the module in its simplest form: an action group with a short name and a single
email receiver.

All Azure resources in this example, including the supporting resource group, are created with
the `Azure/azapi` provider.
